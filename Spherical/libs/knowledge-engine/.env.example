# Knowledge Engine (SQLite + FAISS)
# Copy to .env and adjust values for your local dev environment.

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8085

# Database (SQLite for dev)
DATABASE_URL=sqlite:/tmp/knowledge-engine.db

# Vector store (FAISS index file path)
VECTOR_ADAPTER=faiss
FAISS_INDEX_PATH=/tmp/knowledge-engine.faiss

# Cache (memory by default; set REDIS_URL to enable Redis)
# REDIS_URL=redis://localhost:6380

# Embeddings (OpenRouter)
EMBEDDING_MODEL=qwen/qwen3-embedding-8b
OPENROUTER_API_KEY=your-openrouter-api-key

# Ingestion
PDF_EXTRACTOR_PATH=../pdf-extractor/cmd/pdf-extractor

# Observability / tracing
LOG_LEVEL=debug
LOG_FORMAT=json
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# OTEL_SERVICE_NAME=knowledge-engine

# Auth (leave disabled for local dev)
AUTH_ENABLED=false
# OAUTH2_ISSUER=https://auth.spherical.local
